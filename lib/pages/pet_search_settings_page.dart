import 'package:flutter/material.dart';

class PetSearchSettingsPage extends StatelessWidget {
  const PetSearchSettingsPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Pet Search Settings')));
}
