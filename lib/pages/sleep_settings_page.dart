import 'package:flutter/material.dart';

class SleepSettingsPage extends StatelessWidget {
  const SleepSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Sleep Settings')));
  }
}
