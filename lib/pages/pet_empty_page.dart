import 'package:flutter/material.dart';

class PetEmptyPage extends StatelessWidget {
  const PetEmptyPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Pet Empty')));
}
